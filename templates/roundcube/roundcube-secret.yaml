{{- if .Values.roundcube.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mail-platform.fullname" . }}-roundcube-secret
  labels:
    {{- include "mail-platform.roundcube.labels" . | nindent 4 }}
type: Opaque
data:
  # Database password for Roundcube (base64 encoded)
  # Default: "roundcube123" - CHANGE THIS IN PRODUCTION!
  db-password: cm91bmRjdWJlMTIz
  
  # DES key for encryption (base64 encoded)
  # Generate with: openssl rand -base64 24
  des-key: cmFuZG9tMjRjaGFyc3RyaW5n
{{- end }}
